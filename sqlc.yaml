version: "2"
sql: 
  - schema: "sql/schema"
    queries: "sql/queries"
    engine: "postgresql"
    gen:
      go:
        package: "db"
        out: "jobmgmt/internal/db"
        emit_json_tags: true